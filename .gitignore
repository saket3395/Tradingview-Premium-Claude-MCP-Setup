node_modules/
.env
.DS_Store
data/journal.json
data/upstox_instruments.json
data/upstox_token.json
*.log

# Local TLS certs (mkcert) — never commit private keys
certs/

# Local editor / preview launch config (machine-specific paths)
.claude/*
# ...but keep the committed codebase map (codebase-mapper output)
!.claude/.codebase-info/
!.claude/.codebase-info/**
data/journal.backup-*.json
data/history_cache.json
